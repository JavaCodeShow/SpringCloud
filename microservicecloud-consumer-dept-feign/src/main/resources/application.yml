server:
  port: 8004

# 开启feign客户端对hystrix的支持。不然不会生效。
feign:
  hystrix:
    enabled: true

#eureka的服务注册地址
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka/
spring:
  application:
    name: microservicecloud-dept-consumer
